<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/styles/theme.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Contact Us</title>
</head>
<body>
  <nav class="navbar navbar-expand-sm navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand brand-accent" href="/">Supermarket App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/shopping">Shop</a></li>
          <% if (user && user.role === 'admin') { %>
            <li class="nav-item"><a class="nav-link" href="/feedback-status">Feedback (admin)</a></li>
          <% } %>
          <li class="nav-item"><a class="nav-link active" aria-current="page" href="/contact-us">Contact Us</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container page-shell">
    <div class="hero mb-3">
      <h2 class="fw-bold text-dark mb-1">Contact Us</h2>
      <p class="text-muted mb-0">Reach out by email or phone—we’re here to help.</p>
    </div>

    <div class="panel">
      <div class="panel-body">
        <div class="row g-3">
          <div class="col-md-6">
            <div class="card-muted h-100">
              <h5 class="mb-2">Email</h5>
              <p class="mb-1"><%= contact.email %></p>
              <p class="text-muted small mb-0">We respond within 1 business day.</p>
            </div>
          </div>
          <div class="col-md-6">
            <div class="card-muted h-100">
              <h5 class="mb-2">Phone</h5>
              <p class="mb-1"><%= contact.phone %></p>
              <p class="text-muted small mb-0"><%= contact.hours %></p>
            </div>
          </div>
          <div class="col-12">
            <div class="card-muted h-100">
              <h5 class="mb-2">Address</h5>
              <p class="mb-0"><%= contact.address %></p>
            </div>
          </div>
          <% if (user && user.role === 'admin') { %>
            <div class="col-12">
              <div class="card-muted h-100">
                <h5 class="mb-3">Edit contact info (admin)</h5>
                <% if (success_msg && success_msg.length) { %>
                  <div class="alert alert-success alert-glass"><%= success_msg %></div>
                <% } %>
                <% if (error_msg && error_msg.length) { %>
                  <div class="alert alert-danger alert-glass"><%= error_msg %></div>
                <% } %>
                <form method="POST" action="/contact-us" class="row g-3">
                  <div class="col-md-6">
                    <label class="form-label">Email</label>
                    <input type="email" name="email" class="form-control" value="<%= contact.email %>" required>
                  </div>
                  <div class="col-md-6">
                    <label class="form-label">Phone</label>
                    <input type="text" name="phone" class="form-control" value="<%= contact.phone %>" required>
                  </div>
                  <div class="col-12">
                    <label class="form-label">Address</label>
                    <input type="text" name="address" class="form-control" value="<%= contact.address %>" required>
                  </div>
                  <div class="col-12">
                    <label class="form-label">Hours</label>
                    <input type="text" name="hours" class="form-control" value="<%= contact.hours %>" required>
                  </div>
                  <div class="col-12">
                    <button type="submit" class="btn btn-primary btn-rounded btn-glow">Save Contact</button>
                  </div>
                </form>
              </div>
            </div>
          <% } %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
